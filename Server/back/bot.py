import sys

class Bot:
    
    def __init__(self, inital_message = None):
        
        self.inital_message = inital_message
        self.get_inital_message()
        
    def send(message):
        sys.stdout.write(str(message))
    
    def get_inital_message(self):
        
        if not inital_message:
            self.send("NONE")

        # Generate categorical insight response
        
        